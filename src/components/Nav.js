import React from 'react';

function Nav() {
    return (
        <React.Fragment>
            <nav className="Nav">
                Nav works
            </nav>
        </React.Fragment>
    );
}

export default Nav;
